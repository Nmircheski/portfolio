import { TrendColorDirective } from './trend-color.directive';

describe('TrendColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TrendColorDirective();
    expect(directive).toBeTruthy();
  });
});
