import { TrendSignPipe } from './trend-sign.pipe';

describe('TrendSignPipe', () => {
  it('create an instance', () => {
    const pipe = new TrendSignPipe();
    expect(pipe).toBeTruthy();
  });
});
